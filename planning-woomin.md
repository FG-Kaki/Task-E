# 🚀 GitHub Blog Service Specification
작성자 - 우석민
---

## 📌 주요 기능 명세 (Key Features)

### 1. 🛡️ 실시간 단어 필터링 시스템 (Profanity & Word Filter)

> **"지능형 텍스트 스캐닝으로 깨끗한 커뮤니티 및 블로그 환경을 조성합니다."**

#### 📋 개요
블로그 포스트 및 댓글 입력 시 부적절한 언어, 비속어, 지정된 금지어를 **실시간으로 감지하고 차단/변환**하는 보안 및 텍스트 정화 시스템입니다.

#### ⚙️ 세부 동작 방식
* **실시간 패턴 매칭 (Pattern Matching):**
  * 사용자가 작성한 본문 텍스트를 파싱하여 사전 등록된 금지어 라이브러리와 다중 비교합니다.
* **유연한 마스킹 & 차단 정책 (Masking & Blocking):**
  * **일반 모드:** 감지된 금지어를 `***` 형태의 대체 문자열로 자동 치환합니다.
  * **엄격 모드:** 금지어가 포함된 문서/댓글의 저장 요청을 거부하고 알림 에러 메시지를 반환합니다.
* **커스텀 필터링 규칙 제공 (Customization):**
  * 관리자는 `_config.yml` 또는 별도의 `filter-rules.json` 설정을 통해 금지어 목록 및 정규식(Regex)을 용이하게 업데이트할 수 있습니다.

---

### 2. 🔗 연관 문서 스마트 추천 엔진 (Related Content Engine)

> **"콘텐츠 간의 맥락을 분석하여 유저의 이탈을 막고 탐색 경험을 극대화합니다."**

#### 📋 개요
사용자가 보고 있는 문서의 주제와 **연관성이 높은 다른 포스트를 자동으로 추출하여 추천**해 주는 문맥 기반 가이드 시스템입니다.

#### ⚙️ 세부 동작 방식
* **다차원 유사도 분석 (Multi-Factor Scoring):**
  * 게시글의 **카테고리(Category)**, **태그(Tags)**, **핵심 키워드(Keywords)**를 가중치 기반으로 산출하여 문서 간 연관성 점수를 계산합니다.
* **동적 연관 포스트 매핑 (Dynamic Recommendation):**
  * 포스트 하단에 연관도가 가장 높은 상위 **3~5개의 포스트 카드**를 세련된 UI로 자동 노출합니다.
* **사용자 체류 시간 향상 (SEO & Engagement):**
  * 관련 콘텐츠로의 자연스러운 핑퐁 이동을 유도하여 방문자의 페이지 체류 시간(Dwell Time)을 극대화하고 이탈률을 감소시킵니다.

---

## 🛠️ 기술 요약 (Tech Specs)

| 구분 | 단어 필터링 시스템 | 연관 문서 추천 엔진 |
| :--- | :--- | :--- |
| **적용 범위** | 게시글 본문, 댓글, 사용자 프로필 | 포스트 상세 페이지 하단 |
| **주요 알고리즘** | Aho-Corasick / Regex Pattern Match | TF-IDF / Tag-based Cosine Similarity |
| **설정 관리** | `config/filter-rules.json` | Meta-data Front Matter (YAML) |